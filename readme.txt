This project executes the Jenkins build for java projet and automates the build after every 1 minute whenever the changes occur. So, after adding the files the build automates to check if the program is running succesfully or not.

Hence Jenkins is used to find if, we require a particular process to automate after fixed interval of time
 
 
 
 
 
 
 
 
 
 
 
 
 
 
